export enum Color {
  primary = "--primary",
  darkPrimary = "--dark-primary",
  secondary = "--secondary",
  tertiary = "--tertiary",
  darkestGray = "--darkest-gray",
  white = "--white",
  transparent = "--transparent",
}

export enum Gap {
  xs = "--gap-xs",
  sm = "--gap-sm",
  md = "--gap-md",
  lg = "--gap-lg",
  xl = "--gap-xl",
}
