export enum InputType {
  text = "text",
  number = "number",
}

export enum ButtonType {
  submit = "submit",
  button = "button",
}

export enum IconName {
  trash = "trash",
}

export enum Size {
  sm = "small",
  md = "medium",
}
